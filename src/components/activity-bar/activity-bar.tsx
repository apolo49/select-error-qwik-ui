import { component$ } from "@builder.io/qwik";

export const ActivityBar = component$(() => {

  return (
    <aside style="background-color: pink; position: fixed; height:100%; width:48px">
    </aside>
  );
});
